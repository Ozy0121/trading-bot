---
phase: 09-architecture-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard.py
  - routes/__init__.py
  - routes/trading.py
  - routes/scanner.py
  - routes/data.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 35+ routes still respond after dashboard split"
    - "Server starts without import errors"
    - "SSE stream still pushes state updates"
    - "Dashboard HTML page still renders at /"
  artifacts:
    - path: "routes/__init__.py"
      provides: "Package marker for route modules"
    - path: "routes/trading.py"
      provides: "Flask Blueprint with trading routes"
      contains: "trading_bp = Blueprint"
    - path: "routes/scanner.py"
      provides: "Flask Blueprint with scanner/prediction routes"
      contains: "scanner_bp = Blueprint"
    - path: "routes/data.py"
      provides: "Flask Blueprint with core/data routes"
      contains: "data_bp = Blueprint"
    - path: "dashboard.py"
      provides: "Thin coordinator that registers Blueprints"
      contains: "app.register_blueprint"
  key_links:
    - from: "routes/trading.py"
      to: "dashboard.py"
      via: "lazy import of _trading_client inside route functions"
      pattern: "from dashboard import _trading_client"
    - from: "dashboard.py"
      to: "routes/trading.py"
      via: "Blueprint registration"
      pattern: "app.register_blueprint(trading_bp)"
---

<objective>
Split the monolithic dashboard.py (1,086 lines, 35+ routes) into three Flask Blueprint modules per D-01/D-02: trading routes, scanner/prediction routes, and core/data routes.

Purpose: Reduce cognitive load and merge conflict risk by separating route concerns into focused modules. This is the prerequisite for Phase 12 (Dashboard UX Overhaul).

Output: dashboard.py reduced to thin coordinator (~100 lines); three new Blueprint files in routes/; all existing routes functional.
</objective>

<execution_context>
@C:\Users\btuo1\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\btuo1\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-architecture-cleanup/09-CONTEXT.md
@.planning/phases/09-architecture-cleanup/09-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs to replicate -->

From dashboard.py — dependency injection globals (lines 47-52):
```python
_trading_client = None
_data_client    = None
_kill_fn        = None
_start_fn       = None
_coordinator    = None
```

From dashboard.py — set_dependencies (called by server.py line 93):
```python
def set_dependencies(trading_client, data_client, kill_fn, start_fn):
    global _trading_client, _data_client, _kill_fn, _start_fn
    _trading_client = trading_client
    _data_client    = data_client
    _kill_fn        = kill_fn
    _start_fn       = start_fn
```

From dashboard.py — set_coordinator (called by server.py line 109):
```python
def set_coordinator(coordinator):
    global _coordinator
    _coordinator = coordinator
```

Blueprint lazy-import pattern (avoid circular imports):
```python
# INSIDE route handler functions, NOT at module top level:
from dashboard import _trading_client
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three Blueprint route modules and move routes</name>
  <files>routes/__init__.py, routes/trading.py, routes/scanner.py, routes/data.py</files>
  <read_first>dashboard.py</read_first>
  <action>
Create `routes/` directory with `__init__.py` (empty file).

Create `routes/trading.py` with a Blueprint named `trading_bp = Blueprint("trading", __name__)`.
Move these route handlers from dashboard.py into trading_bp:
- POST /api/order (api_order)
- DELETE /api/order/<order_id>/cancel (api_cancel_order)
- POST /api/start (api_start)
- POST /api/stop (api_stop)
- POST /api/kill (api_kill)
- POST /api/sell_all (api_sell_all)
- POST /api/config/exits (api_config_exits)
- GET /api/positions (api_positions)
- GET /api/orders (api_orders)
- GET /api/orders/history (api_orders_history)
- GET /api/account (api_account)
- GET /api/performance (api_performance)
- GET /api/backtest/* (all backtest routes)
- POST /api/predictions/auto-trade (api_predictions_auto_trade)

Top-level imports for routes/trading.py:
```python
from flask import Blueprint, jsonify, request, abort
import config
import state as shared_state
from logger_setup import get_logger
log = get_logger()
trading_bp = Blueprint("trading", __name__)
```
All references to `_trading_client`, `_data_client`, `_kill_fn`, `_start_fn`, `_coordinator` MUST use lazy imports INSIDE handler functions: `from dashboard import _trading_client`. Never import these at module top level (circular import risk per RESEARCH.md Pitfall 1).

Create `routes/scanner.py` with `scanner_bp = Blueprint("scanner", __name__)`.
Move these route handlers:
- GET /api/predictions (api_predictions)
- POST /api/predictions/run (api_predictions_run)
- GET /api/overnight (api_overnight)
- GET /api/overnight/accuracy (api_overnight_accuracy)
- POST /api/expanded-scan/run (api_expanded_scan_run)
- GET /api/expanded-scan (api_expanded_scan)
- GET /api/scan (api_scan)
- GET /api/intelligence (api_intelligence)
- GET /api/heatmap/* (all heatmap routes)
- GET /api/scan-logs (api_scan_logs)
- GET /api/prediction-log/* (all prediction-log routes)

Top-level imports same pattern as trading.py. Lazy import `_trading_client`, `_data_client`, `_coordinator` inside handlers that need them.

Create `routes/data.py` with `data_bp = Blueprint("data", __name__)`.
Move these route handlers:
- GET / (index — renders template)
- GET /api/state (api_state)
- GET /api/stream (api_stream — SSE endpoint)
- GET /api/health (api_health)
- GET /api/quote/<symbol> (api_quote)
- GET /api/bars/<symbol> (api_bars)
- GET /api/agents/status (api_agents_status)

For the SSE stream in data.py: the generator function that yields events should be moved along with its route. Import `shared_state.snapshot()` at module level (no circular import — state.py does not import dashboard).

Preserve ALL existing imports each handler needs. When a handler imports from another module (e.g., `from prediction_scanner import get_latest_predictions`), keep that import inside the handler function (matching existing codebase pattern).
  </action>
  <verify>
    <automated>python -c "from routes.trading import trading_bp; from routes.scanner import scanner_bp; from routes.data import data_bp; print('All blueprints import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - routes/__init__.py exists (can be empty)
    - routes/trading.py contains `trading_bp = Blueprint("trading", __name__)`
    - routes/scanner.py contains `scanner_bp = Blueprint("scanner", __name__)`
    - routes/data.py contains `data_bp = Blueprint("data", __name__)`
    - routes/trading.py contains `@trading_bp.route("/api/order"` 
    - routes/scanner.py contains `@scanner_bp.route("/api/predictions"`
    - routes/data.py contains `@data_bp.route("/api/state")`
    - routes/data.py contains `@data_bp.route("/api/stream")`
    - No top-level `from dashboard import _trading_client` in any routes/*.py file
    - Every `from dashboard import` appears inside a def block (lazy import)
  </acceptance_criteria>
  <done>All three Blueprint modules created with correct route assignments per D-01. No circular imports at module level. All handler logic preserved exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Slim dashboard.py to coordinator and register Blueprints</name>
  <files>dashboard.py</files>
  <read_first>dashboard.py, routes/trading.py, routes/scanner.py, routes/data.py</read_first>
  <action>
Rewrite dashboard.py to be a thin coordinator (~80-120 lines). It should contain ONLY:
1. Flask app creation: `app = Flask(__name__)`
2. The 5 dependency globals: `_trading_client`, `_data_client`, `_kill_fn`, `_start_fn`, `_coordinator`
3. `set_dependencies()` function (unchanged)
4. `set_coordinator()` function (unchanged)
5. Blueprint registration:
```python
from routes.trading import trading_bp
from routes.scanner import scanner_bp
from routes.data import data_bp

app.register_blueprint(trading_bp)
app.register_blueprint(scanner_bp)
app.register_blueprint(data_bp)
```
6. `run()` function (unchanged)
7. Any shared helper functions used by multiple Blueprint modules (e.g., `_fmt_order()` if used by both trading.py and data.py). These stay in dashboard.py and are lazy-imported by the route modules.

Remove ALL `@app.route` decorators from dashboard.py — they now live in the Blueprint modules.

Keep these imports in dashboard.py:
```python
import logging
from flask import Flask
import config
import state as shared_state
from logger_setup import get_logger
```

Suppress werkzeug logging as before:
```python
logging.getLogger("werkzeug").setLevel(logging.ERROR)
```

Verify that `server.py` does NOT need changes — it already calls `dashboard.set_dependencies()`, `dashboard.set_coordinator()`, and `dashboard.run()`, which all remain in dashboard.py.
  </action>
  <verify>
    <automated>cd C:/Users/btuo1/trading-bot && python -c "import dashboard; print(f'dashboard.py lines: {len(open(\"dashboard.py\").readlines())}'); print('app object:', type(dashboard.app)); print('set_dependencies:', callable(dashboard.set_dependencies)); print('Blueprints:', [bp.name for bp in dashboard.app.blueprints.values()])"</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.py contains `app.register_blueprint(trading_bp)`
    - dashboard.py contains `app.register_blueprint(scanner_bp)`
    - dashboard.py contains `app.register_blueprint(data_bp)`
    - dashboard.py contains `def set_dependencies(`
    - dashboard.py contains `def set_coordinator(`
    - dashboard.py contains `def run(`
    - dashboard.py does NOT contain `@app.route` (all routes moved to Blueprints)
    - dashboard.py is under 150 lines (was 1,086)
    - `python -c "import dashboard"` exits with code 0 (no import errors)
  </acceptance_criteria>
  <done>dashboard.py is a thin coordinator under 150 lines. All routes live in Blueprint modules. server.py integration unchanged. No `@app.route` decorators remain in dashboard.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Flask routes | Untrusted HTTP input from dashboard users (localhost only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T (Tampering) | Blueprint route registration | accept | Routes are purely reorganized, no new endpoints added. Same localhost-only trust model. |
| T-09-02 | I (Information Disclosure) | Lazy imports expose globals | accept | `_trading_client` etc. were already module globals in dashboard.py; Blueprint modules access them the same way via lazy import. No new exposure. |
</threat_model>

<verification>
1. `python -c "from routes.trading import trading_bp; from routes.scanner import scanner_bp; from routes.data import data_bp; print('OK')"` exits 0
2. `python -c "import dashboard; print(len(dashboard.app.url_map._rules))"` shows 35+ registered routes
3. dashboard.py has zero `@app.route` decorators
4. No top-level `from dashboard import _` in routes/*.py files
</verification>

<success_criteria>
- dashboard.py reduced from 1,086 lines to under 150 lines
- Three Blueprint modules created in routes/ with all routes migrated
- Server starts without import errors
- All routes respond (manual smoke test with `py server.py paper`)
</success_criteria>

<output>
After completion, create `.planning/phases/09-architecture-cleanup/09-01-SUMMARY.md`
</output>
