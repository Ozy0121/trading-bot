---
phase: quick-260402-e9l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard.py
  - bot.py
  - server.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "POST /api/stop stops the bot polling loop but does NOT liquidate positions or cancel bracket orders"
    - "SIGINT/SIGTERM stops the bot polling loop but does NOT liquidate positions or cancel bracket orders"
    - "POST /api/sell_all still liquidates all positions AND stops the bot (unchanged)"
    - "coordinator.run_cycle() is called each iteration of bot.py's main loop"
    - "coordinator.startup_sequence() runs before the main loop begins"
    - "coordinator.shutdown_sequence() runs when the bot stops"
  artifacts:
    - path: "dashboard.py"
      provides: "/api/stop route that only stops bot, no liquidation"
    - path: "bot.py"
      provides: "Main loop wired to coordinator.run_cycle()"
    - path: "server.py"
      provides: "Passes coordinator to bot via start_fn"
  key_links:
    - from: "server.py"
      to: "bot.py"
      via: "coordinator passed through run_bot_from_server or module-level setter"
      pattern: "coordinator"
    - from: "bot.py"
      to: "agents/coordinator.py"
      via: "run_cycle() called in while loop"
      pattern: "run_cycle"
---

<objective>
Fix two issues: (1) /api/stop and SIGINT signal handler currently call liquidate_all() which sells all positions — they should only stop the bot polling loop, leaving positions and server-side bracket orders intact. Only /api/sell_all should liquidate. (2) The 6-agent coordinator is created in server.py and wired to the dashboard for status display, but never actually runs — wire coordinator.run_cycle() into bot.py's main loop so agents make trading decisions.

Purpose: Stop destroying positions on bot stop, and activate the agent pipeline that was built but never connected.
Output: Modified dashboard.py, bot.py, server.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@dashboard.py (lines 629-657 — /api/stop route)
@bot.py (lines 60-95 — signal handler and request_shutdown; lines 264-537 — main loop)
@server.py (lines 81-107 — coordinator creation and bot wiring)
@agents/coordinator.py (full file — run_cycle, startup_sequence, shutdown_sequence)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix /api/stop and signal handler to stop without liquidating</name>
  <files>dashboard.py, bot.py</files>
  <action>
**dashboard.py — /api/stop route (lines 629-657):**
Replace the `_do()` inner function. Remove the `from safety import liquidate_all` import and the `liquidate_all(_trading_client)` call. The route should only:
1. Call `_bot.request_shutdown()` to set the shutdown flag
2. Set `shared_state.update(status="stopped")`
3. Log the stop event

The `check_only=true` query param behavior stays unchanged.

The new `_do()` function body should be:
```python
def _do():
    import bot as _bot
    _bot.request_shutdown()
    shared_state.update(status="stopped")
```

Update the response message from `"Stopping bot and liquidating positions."` to `"Stopping bot. Positions and bracket orders left in place."`.

**bot.py — _handle_signal (lines 70-91):**
Remove the `liquidate_all(_trading_client_global)` call and the try/except block around it (lines 86-89). Keep the `check_shutdown_stop_losses` warning block (lines 77-85) — it is informational only. After the warning block, just set status to stopped and exit. The signal handler should:
1. Set `_shutdown_requested = True`
2. Update shared_state status to "stopping"
3. Log warning about unprotected positions (existing check_shutdown_stop_losses block — keep as-is)
4. Update shared_state status to "stopped"
5. `sys.exit(0)`

Also update the docstring at top of bot.py: change "Graceful shutdown: liquidates all positions on Ctrl-C" to "Graceful shutdown: stops polling loop on Ctrl-C (positions and bracket orders left in place)".

**Verify /api/sell_all is unchanged** — it already correctly calls liquidate_all + request_shutdown. Do NOT modify it.
  </action>
  <verify>
    <automated>cd C:/Users/btuo1/trading-bot && python -c "import ast; tree=ast.parse(open('dashboard.py').read()); stops=[n for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name=='api_stop']; body_src=open('dashboard.py').read(); assert 'liquidate_all' not in body_src.split('def api_stop')[1].split('def ')[0], '/api/stop still has liquidate_all'; sell_all_src=body_src.split('def api_sell_all')[1].split('def ')[0]; assert 'liquidate_all' in sell_all_src, '/api/sell_all missing liquidate_all'; print('OK: /api/stop clean, /api/sell_all intact')"</automated>
  </verify>
  <done>/api/stop only calls request_shutdown() — no liquidation. Signal handler only sets shutdown flag — no liquidation. /api/sell_all unchanged and still liquidates.</done>
</task>

<task type="auto">
  <name>Task 2: Wire coordinator.run_cycle() into bot.py main loop</name>
  <files>bot.py, server.py</files>
  <action>
**bot.py — Add coordinator support:**

1. Add a module-level variable: `_coordinator = None`

2. Add a setter function:
```python
def set_coordinator(coordinator) -> None:
    global _coordinator
    _coordinator = coordinator
```

3. In `run_bot()`, right after the `load_state_from_file()` and bracket check block (after line 288), add coordinator startup:
```python
if _coordinator:
    try:
        _coordinator.startup_sequence()
    except Exception as exc:
        log.error("[bot] Coordinator startup failed: %s", exc, exc_info=True)
```

4. In the main `while` loop, after step 7 (scan watchlist, line 343) and before step 8 (find current open positions, line 347), insert the coordinator cycle call. The coordinator's run_cycle() replaces the manual scan+buy logic but we keep the existing logic as a fallback when no coordinator is set:

```python
# ── 7a. Agent coordinator cycle ─────────────────────────────
if _coordinator:
    try:
        cycle_result = _coordinator.run_cycle()
        log.info("[bot] Agent cycle result: %s", cycle_result.get("action", "UNKNOWN"))
    except Exception as exc:
        log.error("[bot] Coordinator cycle error: %s", exc, exc_info=True)
```

This runs the full 6-agent pipeline (Quant + News parallel scan, Strategist decision, Risk Manager review, Executor order placement) each bot cycle. The existing manual entry logic in step 14 still runs as a fallback — the coordinator's Executor handles its own order placement independently.

5. After the main loop exits (after line 536 `shared_state.update(status="stopped")`), add coordinator shutdown:
```python
if _coordinator:
    try:
        _coordinator.shutdown_sequence()
    except Exception as exc:
        log.error("[bot] Coordinator shutdown failed: %s", exc, exc_info=True)
```

**server.py — Pass coordinator to bot:**

After line 97 (`dashboard.set_coordinator(coordinator)`), add:
```python
bot.set_coordinator(coordinator)
```

This wires the coordinator into bot.py so when `run_bot_from_server` starts the main loop, the coordinator is already set.
  </action>
  <verify>
    <automated>cd C:/Users/btuo1/trading-bot && python -c "import ast; src=open('bot.py').read(); assert 'set_coordinator' in src, 'missing set_coordinator'; assert 'run_cycle' in src, 'missing run_cycle'; assert 'startup_sequence' in src, 'missing startup_sequence'; assert 'shutdown_sequence' in src, 'missing shutdown_sequence'; srv=open('server.py').read(); assert 'bot.set_coordinator' in srv, 'server not wiring coordinator to bot'; print('OK: coordinator fully wired')"</automated>
  </verify>
  <done>coordinator.run_cycle() executes each bot polling iteration. startup_sequence() runs before the loop. shutdown_sequence() runs after the loop. server.py passes coordinator to bot module.</done>
</task>

</tasks>

<verification>
1. `python -c "import dashboard; import bot; print('imports OK')"` — both modules parse without error
2. `/api/stop` route code contains no reference to `liquidate_all`
3. `/api/sell_all` route code still contains `liquidate_all`
4. `bot.py` contains `run_cycle`, `startup_sequence`, `shutdown_sequence` calls
5. `server.py` contains `bot.set_coordinator(coordinator)`
6. Signal handler `_handle_signal` contains no `liquidate_all` call
</verification>

<success_criteria>
- Stopping the bot (via /api/stop or Ctrl-C) leaves all positions and bracket orders intact on the broker
- Only /api/sell_all liquidates positions
- The 6-agent coordinator pipeline runs every bot cycle and makes trading decisions
- Existing manual trading logic still works as fallback when coordinator is not set
</success_criteria>

<output>
After completion, create `.planning/quick/260402-e9l-fix-stop-without-liquidate-and-wire-up-a/260402-e9l-SUMMARY.md`
</output>
