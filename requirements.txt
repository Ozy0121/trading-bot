# Alpaca trading SDK (v3+ uses alpaca-py)
alpaca-py>=0.13.0

# Environment variable loading from .env
python-dotenv>=1.0.0

# Numerical operations for moving average calculations
numpy>=1.24.0

# Data manipulation for bar/OHLCV data
pandas>=2.0.0

# Timezone handling for overnight scanner scheduling
pytz>=2023.3

# HTML table parsing for S&P 500 / NASDAQ 100 lists from Wikipedia
lxml>=4.9.0

# Anthropic SDK for multi-agent Claude orchestration
anthropic>=0.40.0

# Unified data layer (currently broken on Python 3.14, using direct yfinance+FMP)
openbb>=4.5.0
